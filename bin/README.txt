Here are pre-compiled binaries for running comparisons (at resolution 640x480).
Don't try to run tests directly from the zip package but decompress the whole
package first. The tests use files from the ..\SVG\ directory.
